# Puerto del servidor (por defecto 3100)
PORT=3100

# Cadena de conexión a PostgreSQL
DATABASE_URL=postgresql://postgres:admin@localhost:5432/control_gastos

# Secreto para firmar tokens JWT
JWT_SECRET=control-gastos-secret-dev

# Duración de la sesión (ej: 3h, 1d)
JWT_EXPIRES_IN=3h